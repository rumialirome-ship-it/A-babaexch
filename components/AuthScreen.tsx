
import React from 'react';
// This component is deprecated and replaced by LandingPage.tsx
// Keeping empty file to prevent build errors if referenced implicitly, 
// but content is removed to allow CSS purge.
const AuthScreen = () => null;
export default AuthScreen;
